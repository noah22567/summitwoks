To start this you must first open a terminal and navigate to the templates page. Then do "pthon -m http.server -b127.0.0.1" to get the webservice up. Then do "python server.py" and "python client.py". within the client.py terminal you can reset the webservice by typing "reset". You can find the fibinacci sequence to a number by doing "fib *arg" the *arg must be an int value. You can disconnect by typing "quit".
